import Callout from './Callout';
import CodePlayground from './CodePlayground';

// 导出所有MDX组件
export const mdxComponents = {
  Callout,
  CodePlayground,
};

// 方便直接导入单个组件
export { Callout, CodePlayground };
